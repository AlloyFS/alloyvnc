[package]
name = "alloyvnc-encode"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "The framebuffer, pixel-format conversion, and the RFB encoders."

[lints]
workspace = true

[dependencies]
alloyvnc-proto.workspace = true
alloyvnc-region.workspace = true
# xxh3 is the fast half of xxhash: a few cycles a byte, which is what
# makes hashing every damaged pixel cheaper than comparing them.
xxhash-rust.workspace = true
